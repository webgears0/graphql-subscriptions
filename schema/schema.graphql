type Query {
  posts: [Post]!
}

type Mutation {
  createPost(input: CreatePostInput!): CreatePostPayload!
}

input CreatePostInput {
  title: String!
  content: String!
}

type CreatePostPayload {
  post: Post!
}

type Post {
  title: String!
  content: String!
  createdAt: String!
  updatedAt: String
}
